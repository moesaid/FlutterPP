import 'package:flutter/material.dart';

class ProjectIndexPage extends StatelessWidget {
  const ProjectIndexPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
